{
  "name": "coffee-shop-payment-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "PayOS payment + webhook server cho Coffee Shop — tạo link thanh toán và cập nhật paymentStatus của đơn trên Firestore khi có webhook.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@payos/node": "^2.0.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.7.0"
  }
}
